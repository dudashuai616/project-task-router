# 直接交给 AI 的安装提示词

把本文件连同整个分享包一起交给 AI，然后发送下面这段话：

```text
请把附件中的 `project-task-router` Skill 和 `AGENTS－任务复杂度门检片段.md` 作为一套完整功能安装到你自己的运行环境。

请直接执行以下步骤：
1. 完整阅读两个部分，不要只看摘要。
2. 找到你真实的全局 Skill 目录，把整个 `project-task-router` 文件夹复制进去。
3. 找到你的全局 `AGENTS.md` 或等效长期规则文件，只把 AGENTS 片段中的“规则正文”合并进去；保留并去重原有规则，不要整份覆盖。
4. 安装后的配置必须引用你自己的正式目录，不要长期引用这份分享包。
5. 重新读取已安装的 Skill 和长期规则，确认两者都已生效。
6. 最后告诉我：Skill 的实际安装路径、修改的长期规则文件、验证结果和未完成事项。

如果你没有写入权限、找不到对应目录，或者规则发生冲突，请准确说明并询问我，不要假装安装成功。
```


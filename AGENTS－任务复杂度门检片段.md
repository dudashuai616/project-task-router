# 可加入另一 Agent 长期规则的任务复杂度门检片段

下面内容是可移植规则片段。安装时应把“规则正文”合并到另一 Agent 自己的长期 `AGENTS.md` 或等效长期规则文件中，不要覆盖对方原有的无关规则。

## 规则正文

```markdown
# 任务复杂度门检与对话接力

- 每次收到新的实质性任务，先检查当前已安装的 Skill 是否适用，再做一次轻量复杂度门检；不要为了展示流程而机械调用 Skill，也不要把简单任务人为拆复杂。
- **简单任务**：只有一个清楚成果、依赖很少、无需长期交接。直接执行，不展示多余计划。
- **聚焦多步任务**：围绕一个主要成果，有少量相互关联的步骤。先给出 2～5 步简短计划，然后在当前对话完成。
- **多阶段任务**：涉及多个成果、不同资料集、核心文件或明显的上下游依赖。大量执行前先使用已安装的 `project-task-router` Skill；说明总目标、阶段、对话边界、准确输入、正式输出、完成标准和交接更新。
- **长期任务**：可能跨多个会话、积累大量决策，或出现上下文可靠性风险。先确定正式资料来源、阶段状态和交接方式，再开始大量执行。
- 只有在拆分确实能降低上下文负担、返工或歧义时，才建议转入新对话；紧密耦合、能在小范围资料内完成的工作应留在当前对话。
- 不得仅凭“步骤多”就机械拆分。简单任务直接做；单一成果的相关步骤尽量在同一对话闭环。
- 把对话当作工作空间，不当作唯一档案。需要保留的确认成果应写入清楚命名的正式文件，并放在内容真正所属的位置。
- 转入新对话、暂停未完成工作，或发现上下文可靠性下降前，必须先保存正式成果，再生成精简交接和可直接使用的续接提示。
- 精简交接至少包含：任务与当前阶段、已完成且确认的内容、已锁定决定、正式文件路径和版本、未完成事项与阻碍、下一步动作、下一对话需要读取的准确文件、可直接使用的续接提示。
- 持续观察上下文风险，不假装知道精确的剩余容量或百分比。若早期内容已被摘要、关键原文无法可靠回看、重要约束难以核对、修订与文件显著增多，或阶段转换需要完全不同的资料，应及时说明风险并先整理可续接的关键状态。
- 新对话应从正式文件和精简交接继续，不要求重新阅读冗长旧对话，也不依赖隐藏推理或废弃草稿恢复项目状态。
- 除非用户明确要求，或适用规则明确要求，否则不要自行创建、分叉用户拥有的新对话；应提出建议并提供续接提示，由用户决定是否建立新对话。
```

## 合并注意事项

- 若对方已有同类门检规则，应去重合并，避免两套规则互相冲突。
- 若对方的 Skill 名称或安装位置不同，应保留工作逻辑并改成真实可用的名称与路径。
- 本片段只治理任务复杂度、对话边界和交接，不应覆盖对方已有的安全、权限、项目归档或专业领域规则。

